"use client"
import React, { useRef } from 'react'

export default function Gide_read_more({ gide_more }) {
    gide_more = useRef(null)
    return (
        <div ref={gide_more}>
           
        </div>
    )
}
